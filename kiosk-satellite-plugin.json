{
  "schemaVersion": 1,
  "apiVersion": 1,
  "id": "package-management",
  "name": "Package Management",
  "version": "0.2.0",
  "minAndroidSdk": 24,
  "entryClass": "me.jxl.kiosk.plugins.packagemanagement.PackageManagementPlugin",
  "description": "Install or uninstall an APK by URL, update the panel's System WebView, and neutralize intrusive vendor packages. Requires root; no vendor signature pinning or auto-update machinery — you choose what to install.",
  "author": "David Coulson",
  "license": "Apache-2.0",
  "capabilities": [],
  "settings": [
    {
      "key": "installUrl",
      "type": "string",
      "title": "APK to install (URL)",
      "description": "HTTPS URL to an APK. Trigger the Install action below to download and install it.",
      "default": "",
      "group": "Install"
    },
    {
      "key": "uninstallPreset",
      "type": "select",
      "title": "Uninstall: pick a known package",
      "description": "Common vendor/test packages, so you don't have to type one. Run the Uninstall package action to apply. Anything typed into the field below takes priority over this pick. Uninstalling is not reversible without a factory reset or reflash — prefer taming if you only want an app to stop interfering.",
      "options": [
        "None",
        "com.elclcd.otaupdater",
        "com.elclcd.commonkeepalive",
        "com.DeviceTest",
        "com.elc.smt_test",
        "com.cghs.stresstest",
        "com.smatek.test",
        "com.tuya.devicetest"
      ],
      "default": "None",
      "group": "Uninstall"
    },
    {
      "key": "uninstallPackage",
      "type": "string",
      "title": "Package to uninstall",
      "description": "Overrides the dropdown above when non-empty. Use for anything not listed there.",
      "default": "",
      "group": "Uninstall"
    },
    {
      "key": "webviewUrl",
      "type": "string",
      "title": "System WebView APK (URL)",
      "description": "HTTPS URL to a com.android.webview build for this panel's ABI. Trigger the Update WebView action below.",
      "default": "",
      "group": "WebView"
    },
    {
      "key": "tamePreset",
      "type": "select",
      "title": "Tame: preset bundle",
      "description": "One-click bundles of vendor packages known to interfere with kiosk use. Applied in addition to the list below, not instead of it. Taming is reversible: clearing this re-enables whatever it disabled. Entries marked unverified come from ha-paneld's hardware notes and were not confirmed on a panel here — a name that doesn't exist on your model is simply skipped.",
      "options": [
        "None",
        "elclcd panels: OTA updater + keepalive",
        "Factory test apps (verified on test panels)",
        "SmartOS/xinch vendor apps (documented, unverified)",
        "Tuya test app (documented, unverified)"
      ],
      "default": "None",
      "group": "Tame"
    },
    {
      "key": "tameVendorPackages",
      "type": "string",
      "title": "Tamed vendor packages",
      "description": "Extra packages to tame, space or comma separated. Added to the preset above. Critical system packages are always refused.",
      "default": "",
      "group": "Tame"
    },
    {
      "key": "simulation",
      "type": "boolean",
      "title": "Simulation mode",
      "description": "Test the controls without touching hardware or packages.",
      "default": false,
      "group": "Developer Tools"
    }
  ],
  "commands": [
    {
      "id": "installFromUrl",
      "title": "Install APK from URL"
    },
    {
      "id": "uninstallPackage",
      "title": "Uninstall package"
    },
    {
      "id": "updateWebView",
      "title": "Update System WebView"
    },
    {
      "id": "detect",
      "title": "Check root access"
    }
  ]
}
